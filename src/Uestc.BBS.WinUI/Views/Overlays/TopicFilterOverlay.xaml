<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Uestc.BBS.WinUI.Views.Overlays.TopicFilterOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Uestc.BBS.WinUI.Views.Overlays"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Uestc.BBS.WinUI.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:TopicFilterViewModel}"
    mc:Ignorable="d">

    <!--<TextBlock Text="TopicFilterOverlay" />-->

    <ListView
        x:Name="MyListView"
        AllowDrop="True"
        CanDragItems="True"
        DragItemsCompleted="MyListView_DragItemsCompleted"
        DragItemsStarting="MyListView_DragItemsStarting"
        DragOver="MyListView_DragOver"
        Drop="MyListView_Drop">

        <ListView.ItemTemplate>
            <DataTemplate>
                <Border
                    Margin="5"
                    Padding="10"
                    Background="LightGray"
                    BorderBrush="Black"
                    BorderThickness="1">
                    <TextBlock Text="{Binding}" />
                </Border>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</Page>
